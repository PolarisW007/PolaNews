# 一念三千 PolaNews · 开发日志

> 本文档为项目的「开发主日志」，记录从 v1.0 初始发布至今的全部开发节点、重要决策与未决事项。
> 每次开发推进后请在 [§5 变更流水日志](#5-变更流水日志) 顶部新增一条，并视情况更新 [§6 待办与规划](#6-待办与规划)。

- **项目名**：一念三千（PolaNews）— 全球资讯 RSS 聚合阅读平台
- **仓库**：`github.com/PolarisW007/PolaNews`（主分支：`main`）
- **线上地址**：`http://aipd.me/polanews`
- **当前 HEAD**：`main` · 2026-05-17
- **日志最近更新**：2026-05-18
- **最近复核**：2026-05-23（Digest 海报内容源一致性与日期缓存修复，本地构建通过）

---

## 1. 项目概览

一念三千是一个面向个人信息工作者的 AI 资讯聚合平台，核心能力：

- **RSS 全球信源聚合**：覆盖主流科技 / 商业 / 政治 / 财经 / AI 信源，支持 OPML 导入、自动去重、分类路由
- **AI 摘要与翻译**：LLM 摘要 + 中英对照翻译（保留图文结构）+ 语义搜索（pgvector）
- **每日简报（Digest）**：每天 08:00 / 20:00 自动汇总，支持中文版、英文版双语，导航与锚点联动
- **AI 语音播报**：阿里云 CosyVoice 自然语音 TTS，支持进度条、预加载、后台续播
- **社交分享**：X / 小红书 / 朋友圈多平台分享，AI 自动生成海报（html-to-image）、异步图片生成 + 轮询刷新
- **响应式体验**：全站移动端适配，文章详情页手势左右滑动切换上下篇
- **账号体系**：注册 / 登录 / 邮件验证 / 密码管理 / 收藏 / 历史 / 星标 / 搜索

---

## 2. 技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | Next.js 16.1.6 (App Router, Turbopack) + React 19 + TypeScript |
| 样式 | Tailwind CSS + 原子类 + 少量内联 linear-gradient / 玻璃拟态 |
| 数据库 | PostgreSQL 16 + pgvector（语义搜索，可选） |
| 缓存 / 队列 | Redis 7 |
| 容器化 | Docker + docker-compose |
| LLM | 通用 OpenAI 兼容接口（由 `LLM_API_KEY` 配置） |
| TTS | 阿里云 CosyVoice（`scripts/tts_cosyvoice.py`） |
| 邮件 | nodemailer (SMTP) |
| 图片生成 | html-to-image `toPng` DOM 转 PNG，支持 2x pixelRatio |
| 部署 | 阿里云 ECS，basePath = `/polanews` |

### 目录结构要点

```
app/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/                # 17 个 API 路由（articles/feeds/digest/share/auth/tts…）
│   │   ├── page.tsx            # 首页（38.6KB，含 ArticleCard / Hero / Magazine 四种卡片）
│   │   ├── article/            # 文章详情（手势切换 + AI 摘要面板）
│   │   ├── digest/[date]/      # 每日简报详情
│   │   ├── broadcast/          # AI 语音播报
│   │   ├── share/              # 分享中心（X / 小红书 / 朋友圈 + AI 海报）
│   │   └── ... (login/register/saved/history/search/settings/…)
│   ├── components/             # MainLayout / Toast / BroadcastPlayer 等
│   ├── lib/
│   │   ├── ai/                 # LLM 客户端与提示词
│   │   ├── db/                 # PostgreSQL 封装
│   │   ├── queue/              # Redis 队列（图片生成、TTS 异步任务）
│   │   ├── rss/                # RSS 抓取引擎（含调度器、去重、翻译管线）
│   │   └── services/           # 9 个业务服务层
│   └── instrumentation.ts      # 启动钩子：RSS 与 Digest 调度器
├── scripts/tts_cosyvoice.py
└── data/audio/                 # 运行时 mp3 产物（不入 git）
```

---

## 3. 环境配置

| 变量 | 作用 |
|------|------|
| `DATABASE_URL` | PostgreSQL 连接串（含 pgvector 可选） |
| `REDIS_URL` | Redis 队列与缓存 |
| `JWT_SECRET` | 登录态签名 |
| `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 摘要、翻译、分享文案 |
| `TTS_*` | 阿里云 CosyVoice 凭证 |
| `SMTP_*` | 邮件验证发送 |
| `NEXT_PUBLIC_BASE_PATH` | 生产环境 `/polanews`，本地可留空 |

启动命令：

```bash
# 开发
cd app && npm run dev                  # Next.js 16 + Turbopack，默认 3000
# 生产
docker compose up -d                   # 拉起 pg + redis + app
```

---

## 4. 开发阶段里程碑

按时间倒序划分为 4 个大阶段：

### Stage 4 · 稳定性与一致性修复（2026-05 至今）
- 首页卡片「挤压条」问题彻底修复（Link `className="block"` + `cleanText`/`pickDisplayTitle` 体系）
- 分享海报只展示中文信息（`extractChineseLines` + 中文分隔符切片）
- RSS ingest 缓存修复、海报导出稳定性

### Stage 3 · 移动端与分享生态（2026-03-09）
- 全站移动端响应式适配 + 手势滑动
- 分享功能全面升级（X 平台、AI 海报、对外分享页、异步图片生成）
- 首页 API 性能优化（3MB → 30KB）
- 上一篇/下一篇导航

### Stage 2 · AI 能力深化（2026-03-08）
- TTS 升级为阿里云 CosyVoice 自然语音
- AI 摘要 / 全文阅读器 / Digest 渲染 / 设置升级
- 中英对照翻译保留完整 HTML 结构

### Stage 1 · v1.0 初始发布（2026-03-07）
- 平台基础骨架 + 对标 PRD 填充全部空实现
- 阿里云部署适配 + TypeScript 编译清理
- 数据库扩展、停用词、OPML 导入修复

---

## 5. 变更流水日志

> 新增请在**顶部**插入。每条结构：`[日期] <短 hash> 标题` + `变更点` / `影响文件` / `关联问题`。

---

### [2026-05-23] `pending` Digest 海报内容源一致性与日期缓存修复

**目标**：修复 `/polanews/digest/2026-05-22` 海报里的资讯与页面正文列表不一致的问题，并避免指定日期查询被内存缓存返回成其他日期的 digest。

**变更点**：
1. `/api/digests/latest` 命中缓存前校验 `digest_date`，指定日期不再复用错误日期的数据
2. 指定 `digest_date` 查询增加 `ORDER BY created_at DESC`，同一天有多条 digest 时返回最新记录
3. Digest 海报改为从 `full_content` 解析分组与资讯条目，`1. 标题` 和后续摘要会合并成同一张卡片
4. 海报导出改为按真实 DOM `scrollWidth/scrollHeight` 克隆生成，长图高度随完整资讯列表自动扩展

**验证**：目标 `eslint`、`npx tsc --noEmit`、`npm run build` 通过；构建仅保留既有 `data/audio` 动态路径过宽警告。

---

### [2026-05-23] `pending` 线上 LLM mock 污染排查与保护

**目标**：排查线上文章关键要点显示为 `要点1/要点2/要点3` 的原因，恢复受控服务进程，并阻止生产环境继续写入 mock AI 内容。

**变更点**：
1. 发现线上 `3456` 端口被孤儿 `next-server` 占用，Supervisor 管理的 `polanews` 处于 `FATAL`
2. 清理孤儿进程后恢复 Supervisor 进程，确认新进程具备 `LLM_API_KEY`、`LLM_API_BASE`、`LLM_MODEL`
3. 真实重算目标文章时 DashScope 返回 `Arrearage`，说明当前 LLM 账号欠费/不可用，真实 AI 要点无法生成
4. `llm.ts` 增加生产保护：生产环境未配置 `LLM_API_KEY` 时拒绝生成 mock AI 内容，避免再持久化 `[Mock]` 和占位关键要点

**验证**：`npx tsc --noEmit`、目标 `eslint`、`npm run build` 通过；线上服务恢复为 `RUNNING`；目标文章重算请求确认 LLM provider 返回欠费错误。

---

### [2026-05-18] `pending` 订阅源手动抓取与 error 源恢复

**目标**：修复 `/polanews/subscriptions` 中订阅源进入 `error` 后无法通过手动抓取恢复的问题，并提升 RSS 抓取成功率。

**变更点**：
1. RSS 抓取器增加 User-Agent / Accept 请求头、10 秒超时和 IPv4 优先，减少服务器侧 DNS/网络不稳定导致的失败
2. 手动 `/api/feeds/fetch` 默认重试 `error` 源，订阅页使用 `fetch_only` 快路径，单源手动抓取支持按 `feed_id` 重试
3. 抓取成功后会把订阅源恢复为 `active` 并清空 `error_count`，不再只更新 `last_fetched_at`
4. 抓取 API 返回源级统计：attempted / succeeded / failed / recovered / new_articles / feed_errors
5. 订阅源页面新增每个源的手动刷新按钮，error badge 显示累计错误数，顶部“立即抓取”会给出恢复数量
6. Anthropic 原 RSS 地址 404 时增加官网 News 页面兜底提取，避免该源长期无法入库

**验证**：`npx tsc --noEmit`、目标 `eslint`、`npm run build` 通过；服务器侧复测确认 Dev.to、Google AI、Microsoft Dev Blog、CNBC、NPR 等源可被新抓取策略恢复。

---

### [2026-05-18] `pending` 文章摘要连续朗读与关键要点语音缓存

**目标**：文章详情页点击 `朗读摘要` 后，朗读结束自动进入下一篇并继续朗读，实现循环阅读；朗读语音内容增加 AI 生成的关键要点，并按现有音频模式持久化缓存。

**变更点**：
1. 新增 `article-speech` 纯函数，统一生成 TTS 朗读稿：标题、摘要、关键要点
2. 文章详情页朗读结束后保留连续朗读状态，自动下一篇加载完成后默认开启朗读
3. 手动切换上一篇/下一篇/返回/相关文章仍会停止连续朗读，避免非预期后台播放
4. TTS 合成 API 增加 `audio_text_hash` 校验，旧的摘要-only 音频不会误命中缓存
5. 数据库增加 `audio_text_hash` / `audio_text_hash_en` / `audio_text_hash_ja`，音频 URL 与朗读稿 hash 一起持久化
6. 摘要重新生成后清空对应语言音频缓存，后台抓取合成语音时写入新的 hash

**验证**：`npx tsc --noEmit`、目标 `eslint`、`npm run build` 通过；本地 API 合成返回 `url` 与 `text_hash`；纯函数验证朗读稿包含 `关键要点`。

---

### [2026-05-18] `main` 设置页新增主题选择模块并修复 Header 用户显示

**目标**：在设置页增加可预览、可即时应用并可持久化保存的主题选择模块，参考 PolaZhenjing 管理后台的深色质感扩展 PolaNews 主题体系；同时修复登录后右上角只显示图标、不持续显示用户名的问题。

**变更点**：
1. 设置页主题从简单按钮升级为带缩略预览、色板和选中态的主题选择模块
2. 新增 `一念绿`、`真境玄金`、`极夜蓝`、`琥珀暖光`、`清昼`、`跟随系统` 主题选项
3. `globals.css` 增加 `data-theme` CSS 变量组，真正驱动全站背景、文字、边框、强调色与 glow
4. `layout.tsx` 增加首屏主题初始化脚本，读取 `polanews_theme` 减少刷新闪烁
5. Header 背景改用 `--header-bg`，并持续显示登录用户名；未登录状态直接显示可点击的 `登录`
6. 登录、注册、设置保存会同步本地用户缓存并触发 Header 刷新

**验证**：`npx tsc --noEmit`、目标 `eslint`、`npm run build` 通过；本地临时用户通过 `/api/settings` 保存并读取 `theme: zhenjing` 成功。

---

### [2026-05-17] `main` 封装 PolaNews 服务 CLI

**目标**：将现有 `/polanews` HTTP 服务封装为可脚本调用的命令行工具，方便其他应用以稳定 JSON stdout 调用文章、抓取、Digest、播报、分享和 MCP 能力。

**变更点**：
1. 新增 `app/bin/polanews.mjs`，提供 `articles`、`feeds`、`digests`、`broadcasts`、`shares`、`mcp`、`api` 等命令
2. 支持 `--base-url`、`--token`、`POLANEWS_BASE_URL`、`POLANEWS_TOKEN`、`~/.polanews-cli.json` 配置
3. 支持 `login --save` 持久化 JWT，支持 `api METHOD PATH --data/--query` 直调任意 API
4. `app/package.json` 增加 `bin.polanews` 与 `npm run cli`
5. `app/README.md` 补充 CLI 调用示例和其他应用集成方式
6. 补充 `.gitignore` / `app/.npmignore`，避免运行时音频与本地工具缓存进入 CLI 包或 git 提交

**验证**：`node --check app/bin/polanews.mjs` 通过；`npm --prefix app run lint -- bin/polanews.mjs` 通过；`npm --prefix app run build` 通过；线上 `node app/bin/polanews.mjs --base-url http://aipd.me/polanews articles list --limit 1` 返回最新文章 JSON。

---

### [2026-05-17] `a343797` 修复文章摘要朗读重叠与播完自动下一篇

**目标**：解决文章详情页 `朗读摘要` 在切换文章后旧音频不断、下一篇再次朗读声音重叠、朗读完成不自动进入下一篇的问题。

**变更点**：
1. 新增 `stopTTS()` 统一停止 `HTMLAudioElement` 与 `speechSynthesis`
2. 播放对象改为 `audioRef` 单例管理，避免 React state 清理闭包拿到旧音频
3. 所有文章导航入口统一走 `navigateToArticle()`，先停音频再跳转
4. `audio.onended` / `SpeechSynthesisUtterance.onend` 进入 `handleTTSEnded()`，自然播放结束后自动跳到下一篇
5. 手动停止朗读只停止当前音频，不触发自动下一篇

**验证**：`npx tsc --noEmit`、`npx eslint src/app/article/[id]/page.tsx`、`npm run build` 通过；生产 `/polanews/article/...` 浏览器回归验证朗读中切下一篇会停止旧音频，下一页再次朗读不会保留旧状态，自然播放结束路径会自动进入下一篇。

---

### [2026-05-17] `6e90f29` 修复抓取后中文 AI 摘要持久化与播报音频链路

**目标**：修复抓取/手动抓取后未默认生成中文 AI 摘要和语音、文章详情摘要读取错误、指定播报音频/脚本错误的问题。

**变更点**：
1. `runFullIngest` 增加 `summarizeMissingChineseArticles` 阶段：抓取后默认生成并持久化 `articles.ai_summary` / `ai_key_points`
2. 手动抓取 `/api/feeds/fetch` 恢复自动语音合成，返回 `summarized` 与 `audio_synthesized`
3. 文章详情页中文摘要与朗读统一使用 `ai_summary || summary_zh || summary`
4. 未登录进入文章详情不再因 `markRead` 401 自动跳转登录页
5. 播报生成强制中文输出，增加中文兜底脚本和稳健 segments 切分
6. 播报播放器 on-demand TTS 使用 `broadcast.voice_id`，并修复带 basePath 的音频 URL 拼接
7. 修复线上播报 `70dd0591-d9eb-42fa-9dcf-4bb2aea54ae5`：改为 6 段中文播报，6 段均有音频

**验证**：`npx tsc --noEmit`、本地/服务器 `npm run build` 通过；浏览器验证文章详情中文摘要和音频就绪、指定播报中文脚本和播放器正常，console error 为 0。

---

### [2026-05-17] `d28411c` 服务器环境对齐与 PolaNews 正式命名部署

**目标**：将线上 `aipd.me/polanews` 从旧目录 `/opt/WSYWorldOverview`、旧 supervisor 服务 `worldoverview` 迁移为与本地一致的 `/opt/polanews`、`polanews`，并通过 git 保持本地与服务器代码一致。

**变更点**：
1. 新增 `.node-version` 与 `environment.yml`，本地 conda 环境 `pola-news` 已安装 Node `22.22.2`
2. 服务器从 GitHub `main` 克隆 `/opt/polanews`，复用旧 `.env.local`、`app/data`、`share-images`
3. 服务器 Linux 环境执行 `npm ci && npm run build`，避免 macOS 本地构建产物跨平台问题
4. nginx `/polanews` 静态资源和分享图片目录改为 `/opt/polanews/...`
5. supervisor 配置改为 `[program:polanews]`，端口仍为 `3456`，`SHARE_IMAGES_DIR=/opt/polanews/share-images`

**验证**：`nginx -t` 通过，`supervisorctl status polanews` 为 RUNNING，`http://aipd.me/polanews` 返回 `200 OK`，`/polanews/api/articles` 返回成功 JSON；服务器源码包含 `html-to-image`、`Digest 图片海报`、`HTML 海报预览`、`UNTRANSLATED_WHERE` 等最新功能标记。

**备份/回滚**：备份目录为 `/opt/backups/polanews-20260517001731`；旧目录 `/opt/WSYWorldOverview` 暂时保留作为回滚材料。

---

### [2026-05-17] `infra` 线上页面静态资源 404 回归修复

**目标**：修复 `http://aipd.me/polanews` HTML/API 正常但整页前端交互失效的问题。

**根因**：生产 nginx 的 `/polanews/_next/static/` alias 指向 `/opt/polanews/app/.next/standalone/.next/static/`；当前服务器采用直接 `npm run build` 方式，实际 JS/CSS 资源位于 `/opt/polanews/app/.next/static/`，导致浏览器加载 chunks 404、页面无法完成水合。

**修复**：
1. 更新 `/etc/nginx/conf.d/polazj.conf` 与 `/etc/nginx/sites-enabled/aicoacher` 的 `_next/static` alias 为 `/opt/polanews/app/.next/static/`
2. 执行 `nginx -t` 并 reload nginx
3. 保持 supervisor `polanews` 服务运行在 `/opt/polanews/app`，无需重启应用

**验证**：静态 JS chunk 返回 `200 OK`；浏览器打开首页可见 20 条已翻译文章，滚动后增至 40 条；`/digest/2026-05-15` 可见 `Digest 图片海报` 和 `下载 PNG`；`/share` 选择分享项后可见中文分享详情和图片下载区域；console error 为 0。`npx tsc --noEmit` 通过；`npm run lint` 仍受既有 lint 债务阻塞。

---

### [2026-05-10] `c1783fa` Fix PolaNews ingest cache and poster export

**目标**：解决 RSS ingest 重复拉取的缓存失效 + 分享海报 PNG 中混入英文推文引用的问题。

**变更点**：
1. `extractChineseLines` 海报中文过滤 v2：
   - 新增「中文版本 / 中文版」分隔符定位，英文半部整块抛弃
   - 结构性标签过滤（`每日新闻摘要`/`日期：…`/`推文#N`/`微博#N`/纯破折号行）
   - 海报大标题 `posterTitle = lines[0] || share.title`，首条中文要点升格为 H1
   - 详情弹窗轮询 `useEffect` 依赖稳定化（`detailShareId` / `detailShareStatus`）
2. RSS engine 缓存策略修复 (`app/src/lib/rss/engine.ts` +55)
3. articles / translate / feeds API 健壮性增强
4. digest 详情页渲染优化 (+193)
5. 首页 `page.tsx` 二次优化 (+138)

**影响文件**：9 个文件 · +579 / -68
**验证**：Browser Agent 实测通过，8 个卡片全部中文，无英文引用残留

---

### [2026-05-06] `6d39113` 修复首页卡片挤压条问题 + 多项功能增强

**目标**：本地 `localhost:3000/polanews` 与线上 `aipd.me/polanews` 对比，首页文章卡片之间出现空白「挤压条」。

**根因三连**：
1. `isRenderableArticle` 过滤与 `displayTitle` 渲染不一致（零宽字符/全角空格）
2. `<Link>` 默认 `display:inline`，`space-y-4` 垂直节律失效 ← **真正根因**
3. content 中存在 HTML/markdown 残留未清洗

**变更点**：
- 新增 `cleanText()` 统一剥离 HTML、零宽、全角空格
- 新增 `pickDisplayTitle()` / `shouldShowBilingual()`
- 四个文章卡片组件（`ArticleCard` / `ArticleListItem` / `ArticleMagazineHero` / `ArticleMagazineSmall`）四个 `<Link>` 统一加 `className="block"`
- `dedupeById` 去重兜底

**影响文件**：`app/src/app/page.tsx`
**验证**：20 张卡片正常渲染，首两卡间距正常

---

### [2026-03-09] 移动端与分享生态集群（8 个 commit）

| hash | 主题 |
|------|------|
| `951f643` | 修复 PC 端点击失效（mobile drawer `pointer-events-none`） |
| `1ad27aa` | 文章详情页手势左右滑动切换上下篇 |
| `6234969` | 移动端 AI 摘要面板位置调整到操作栏下方 |
| `88dc8bb` | 全站移动端响应式适配 |
| `7aec5fc` | 图片保存到持久化目录，不受 `next build` 影响 |
| `308bc5d` | 图片保存路径修复 + 生成流程健壮性提升 |
| `73e0f9e` | 异步图片生成体验优化（生成中占位 + 自动轮询） |
| `3d524f3` | 分享详情 API 列名修复（`source_url → url`） |
| `2d0c605` | **分享功能全面升级**：X 平台 + AI 图片生成 + 对外分享页 |
| `96268c6` | 上一篇/下一篇按钮布局重构 |
| `553bd49` | 上一篇/下一篇功能修复 |
| `dd247f2` | 文章详情页新增上一篇/下一篇快速切换 |
| `cea34f3` | **首页 API 性能优化：3MB → 30KB**（瘦身字段） |

---

### [2026-03-08] AI 能力深化（8 个 commit）

| hash | 主题 |
|------|------|
| `b0faf34` | 中英对照翻译保留完整 HTML（图片/样式/链接） |
| `9d37eb3` | fulltext API 增加 GET handler（修复 405） |
| `e5a94f4` | LLM 中文摘要提示词 + 密码 API 列名 + Turbopack 根目录 |
| `7f06ef6` | **全平台深度升级**：AI 摘要 / TTS 预加载 / 全文阅读器 / 播报进度 / Digest 渲染 / 设置 |
| `8ddb4ab` | `BroadcastPlayer` 音频 URL 添加 basePath 前缀 |
| `91526e0` | `Response` + type assertion 返回二进制音频 |
| `3c17a36` | Buffer 转 Uint8Array 兼容 NextResponse |
| `677f277` | **TTS 升级阿里云 CosyVoice 自然语音** |

---

### [2026-03-07] v1.0 初始发布（5 个 commit）

| hash | 主题 |
|------|------|
| `2d85f17` | initial release of PolaNews - AI-powered global news aggregator |
| `4bb3f1f` | 修复 subscriptions page onClick 缺失闭合大括号 |
| `a2850bf` | **一念三千(PolaNews) 全球资讯 RSS 聚合阅读平台 v1.0** |
| `de8cbac` | TypeScript 编译错误修复 & 阿里云部署适配 |
| `ad21d54` | **填充所有空实现，全面对标 PRD** |
| `ed86057` | 数据库扩展权限、停用词过滤、OPML 导入格式 |

---

## 6. 待办与规划

### 🟢 已完成（近期）
- [x] 海报分享只显示中文内容
- [x] 首页卡片挤压条
- [x] RSS ingest 缓存
- [x] 移动端完整适配

### 🟡 进行中 / 可观察
- [ ] pgvector 语义搜索在生产环境启用（目前本地降级为禁用）
- [ ] Digest 自动翻译双语一致性（`中文版本` 标记可能不稳定，依赖 LLM 输出）

### 🔵 规划中
- [ ] 分享海报模板多版（除暗金外，新增简约白 / 杂志风）
- [ ] 个性化推荐（基于用户星标 + 阅读历史）
- [ ] WebPush 通知订阅热点
- [ ] RSS 信源健康度看板（失败率、响应时间）
- [ ] 多语言扩展（日文 / 西语信源）

### 🔴 风险 / 技术债
- `.qoder/` 与 `app/data/audio/` 为运行时未跟踪目录，需确保 `.gitignore` 持续覆盖
- `html-to-image` 对字体、`box-shadow`、`drop-shadow` 存在兼容性问题（详见[前端渲染兼容性记忆]）
- Next.js 16 + Turbopack dev cache 偶发文章旧数据，`rm -rf .next` 为通用兜底

---

## 7. 测试与文档索引

根目录测试报告：

| 文件 | 内容 |
|------|------|
| [产品需求文档_全球资讯RSS聚合阅读平台.md](产品需求文档_全球资讯RSS聚合阅读平台.md) | PRD 主文档（45KB） |
| [README.md](README.md) | 项目说明 |
| [README_测试文档.md](README_测试文档.md) | 测试文档总览 |
| [测试报告_一念三千平台_2026-03-07.md](测试报告_一念三千平台_2026-03-07.md) | v1.0 发布后首轮测试 |
| [测试执行摘要_一念三千平台_2026-03-08.md](测试执行摘要_一念三千平台_2026-03-08.md) | AI 升级后执行摘要 |
| [深度测试报告_一念三千平台_2026-03-08.md](深度测试报告_一念三千平台_2026-03-08.md) | 深度测试 24KB |
| [测试截图总结_一念三千平台_2026-03-08.md](测试截图总结_一念三千平台_2026-03-08.md) | 测试截图归档 |
| [注册登录测试报告_2026-03-08.md](注册登录测试报告_2026-03-08.md) | 邮件验证专项 |

---

## 8. 日志维护约定

**每次开发推进后**请按以下格式在 [§5 变更流水日志](#5-变更流水日志) **顶部**新增一条：

```markdown
### [YYYY-MM-DD] `<短 hash>` <提交标题>

**目标**：...
**变更点**：
- ...
**影响文件**：<文件数> · +<add> / -<del>
**验证**：<本地 / 线上 / Browser Agent / 测试报告链接>
**关联**：<issue / PR / 需求 / 前一 commit>
```

**大版本里程碑**则在 [§4 开发阶段里程碑](#4-开发阶段里程碑) 顶部新增 Stage，简述目标与产出。

**待办状态变化**及时同步 [§6 待办与规划](#6-待办与规划)，已完成项请打勾 + 保留到本次 sprint 结束后再归档。

**代码规模变化**请同步刷新 [§9 代码规模统计](#9-代码规模统计)：每次合并大改动后，运行命令重新生成快照与阶段增量。

---

## 9. 代码规模统计

> 数据时间：2026-05-10（HEAD `c1783fa`）。最近复核：2026-05-05 重跑脚本结果一致。统计范围：`app/src/**` + `app/scripts/**`，排除 `node_modules`、`.next`、`data/`、`public/`。

### 9.1 当前代码快照

**按文件类型**

| 类型 | 文件数 | 行数 | 说明 |
|------|-------:|-----:|------|
| TypeScript (`.ts`) | 70 | 6,391 | 业务逻辑、API 路由、库 |
| React TSX (`.tsx`) | 25 | 8,191 | 页面与组件 |
| CSS | 1 | 93 | 全局样式 (`globals.css`) |
| Python | 1 | 49 | TTS 脚本 (`tts_cosyvoice.py`) |
| **合计** | **97** | **14,724** | — |

**按目录**

| 目录 | 文件数 | 行数 | 占比 |
|------|-------:|-----:|-----:|
| `app/src/app/`（页面 + API 路由） | 69 | 10,706 | 72.7% |
| `app/src/lib/`（业务库 / RSS / AI / DB） | 21 | 3,087 | 21.0% |
| `app/src/components/`（共享组件） | 5 | 875 | 5.9% |
| `app/scripts/`（独立脚本） | 1 | 49 | 0.3% |
| **合计** | **96** | **14,717** | 100% |

> 注：两表合计行数差 7 行，源于按扩展名统计含 `instrumentation.ts` 等顶层文件，按目录归类时存在边界差。可忽略。

### 9.2 阶段增量（git 累计）

| 阶段 | 时间 | Commits | 文件次 | 新增 (+) | 删除 (-) | 净增 |
|------|------|--------:|-------:|---------:|---------:|-----:|
| Stage 1 · v1.0 初始发布 | 2026-03-07 | 6 | 211 | +39,949 | -197 | **+39,752** |
| Stage 2 · AI 能力深化 | 2026-03-08 | 8 | 44 | +6,045 | -742 | **+5,303** |
| Stage 3 · 移动端与分享生态 | 2026-03-09 | 13 | 43 | +1,885 | -732 | **+1,153** |
| Stage 4a · 首页挤压条修复 | 2026-05-06 | 1 | 33 | +941 | -371 | **+570** |
| Stage 4b · ingest 缓存与海报 | 2026-05-10 | 1 | 9 | +579 | -68 | **+511** |
| **总计** | — | **29** | **340** | **+49,399** | **-2,110** | **+47,289** |

> Stage 1 包含 `a2850bf`「v1.0 整体发布」与 `ad21d54`「填充全部空实现对标 PRD」两个超大体量提交，占总新增行的 80%+。

### 9.3 演进观察

- **代码量曲线**：v1.0 一次性奠定 ~40K 行骨架；后续 4 个阶段共增量 ~7.5K 行（增量 ~19%），删除 ~1.9K 行（重构占比健康）。
- **TSX vs TS 比 ≈ 1.28**：UI 与业务库平衡良好（页面 8.2K / 库 6.4K）。
- **页面层占比 72.7%**：因 App Router 把页面 + API 全部放在 `src/app/` 下，正常分布。
- **5 月份回归性修复**：两次共 `+1,520 / -439`，纯净增 1,081 行（含 `extractChineseLines` 重写、`engine.ts` 缓存修复、digest 详情优化）。

### 9.4 重新生成方法

```bash
# 当前快照（按扩展名）
for ext in ts tsx css py; do
  files=$(find app/src app/scripts -name "*.$ext" | wc -l | tr -d ' ')
  lines=$(find app/src app/scripts -name "*.$ext" | xargs wc -l 2>/dev/null | tail -1 | awk '{print $1}')
  echo "$ext: $files files, $lines lines"
done

# 阶段增量（按日期分组）
git log --pretty=tformat:'BOUNDARY %h %ad' --date=short --shortstat --no-merges \
  | awk '/^BOUNDARY/{date=$3;next}
         /file.*changed/{a=0;d=0;f=0;
           for(i=1;i<=NF;i++){if($i~/insertion/)a=$(i-1);
             else if($i~/deletion/)d=$(i-1);
             else if($i~/file/)f=$(i-1)}
           A[date]+=a;D[date]+=d;F[date]+=f;C[date]++}
         END{for(d in C) printf "%s: %d commits, %d files, +%d/-%d\n",d,C[d],F[d],A[d],D[d]}' \
  | sort
```

---

*本文档由 AI 助手基于 git log + 代码 + 文档自动生成，后续由人工持续维护。*
